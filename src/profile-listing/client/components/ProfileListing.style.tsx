import styled from "styled-components";

export const ProfileListingWrapper = styled.div`
  h2 {
    font-size: var(--title);
    background-color: var(--secondary-color);
    color: var(--primary-color);
    height: 200px;
    display: flex;
    justify-content: center;
    align-items: center;
  }
`;
