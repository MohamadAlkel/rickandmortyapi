import React, { FC, ReactElement } from "react";
import ProfileListing from "../../profile-listing";

const ProfileListingPage: FC<any> = (): ReactElement<void> => {
  return <ProfileListing />;
};

export default ProfileListingPage;
